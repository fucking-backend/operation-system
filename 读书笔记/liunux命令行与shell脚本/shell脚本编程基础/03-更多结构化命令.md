```bash
# for命令
# 特殊符号使用转义字符
for value in a b c d e I\'m joke "New York"
do 
	echo The next value is $value
done
echo current value is $value # 迭代结束之后，变量值会保留

# 从变量读取列表
list="a b c d"
list=$list" e"
for value in $list
do 
	echo The next value is $value
done

# 从命令读取值
file="state.txt"
for value in $(cat $file)
do 
	echo The next value is $value
done

# 更改字段分隔符 IFS
file="state.txt"
IFS=$'\n' # 这里要用单引号，双引号遇到n依旧会换行。"2 nd"这种情况
for value in $(cat $file)
do 
	echo The next value is $value
done

# 保证后续操作依旧使用默认值
IFS.OLD=$IFS
IFS=$'\n'
IFS=$IFS.OLD

# 制定多个IFS。这个赋值将换行符、冒号、分号、双引号作为字段分割值
# IFS=$'\n':;"

# 用通配符读取目录，也可以列出多个目录
for value in ~/Desktop/* ~/Desktop/fubao-learning/* ~/Desktop/fubao
do
	if [ -d "$value" ] # 文件名和目录名可以包含空格，所以将$value用双引号包起来。否则遇到空格名会报错
	then
		echo "$value is a dictionary"
	elif [ -f "$value" ]
	then
		echo "$value is a file"
	fi
done


# C语言风格的for命令
for (( i=0;i<=10;i++ ))
do
	echo "The number is $i"
done

# 使用多个变量
for (( a=0,b=10;a<=10;a++,b-- ))
do
	echo "a=$a b=$b"
done



# while
value=6
while [ $value -gt 0 ]
do
	echo $value
	value=$[ $value-1 ]  # question 这里为什么这么写
done

# until
value=100
until [ $value -eq 0 ]
do
	echo $value
	value=$[ $value-25 ]
done


# 嵌套循环，没什么好讲的


# 循环处理文件数据
IFS.OLD=$IFS
IFS=$'\n'
for entry in $(cat /etc/passwd)
do
	echo "Valus in $entry ======"
	IFS=:
	for value in $entry
	do 
		echo "   $value"
	done
done


# 控制循环，break，break n，contine


# 处理循环的输出。可以直接【重定向或者放入管道】 O(∩_∩)O
for value in ~/Desktop/*
do
	if [ -d "$value" ] # 文件名和目录名可以包含空格，所以将$value用双引号包起来。否则遇到空格名会报错
	then7
		echo "$value is a dictionary"
	elif [ -f "$value" ]
	then
		echo "$value is a file"
	fi
done > output.txt
```